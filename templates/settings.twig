{% import "_includes/forms" as forms %}

{{ forms.lightswitchField({
    id: 'useServerImageOptim',
    name: 'useServerImageOptim',
    label: 'Gebruiker server afbeelding optimalisatie'|t,
    on: settings.useServerImageOptim,
    instructions: 'Uitgebreide, tragere optimalisatie via PHPImageOptim.'|t
}) }}
{{ forms.lightswitchField({
    id: 'useImagickImageOptim',
    name: 'useImagickImageOptim',
    label: 'Gebruiker Imagick afbeelding optimalisatie'|t,
    on: settings.useImagickImageOptim,
    instructions: 'Simpele optimalisatie via Imagick.'|t
}) }}

Action url for task that resaves all elements of one or more specified types: {{ actionUrl('amTools/resaveElements') }}<br>
Param: elementTypes<br>
Options(remove the ones you don't want to use from your url): Asset,User,Entry,MaxtrixBlock,AmSocialPlatform_Group<br>
ex: {{ actionUrl('amTools/resaveElements', {'elementTypes': 'Asset,Entry'}) }}
